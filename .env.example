# Pilgrim's Path — Environment Variables (template)
# Copy to .env and fill values. NEVER commit the real .env file.

# === Paystack ===
PAYSTACK_SECRET_KEY=your-paystack-secret-key-here
PAYSTACK_PUBLIC_KEY=your-paystack-public-key-here

# === Anthropic (Claude) ===
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# === Meta CAPI ===
META_CAPI_TOKEN=your-meta-capi-access-token-here
META_DATASET_ID=1267382605021300

# === Supabase (server-side admin operations) ===
SUPABASE_URL=https://giftctxrqvlfekhzpcaa.supabase.co
SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...   # SECRET — server only, never in browser

# === Admin gateway shared secret (used by /api/* to authorise admin calls) ===
# Generate with:  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_TOKEN=replace-with-64-char-hex-secret
ADMIN_EMAIL=admin@pilgrimspath.io

# === Email (SMTP via Gmail App Password) ===
# Generate at https://myaccount.google.com/apppasswords (revoke any password
# that has ever appeared in chat/Slack/git/etc).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=ameenabuhuraira@gmail.com
SMTP_PASS=replace-with-fresh-16-char-app-password
MAIL_FROM="Pilgrim's Path <ameenabuhuraira@gmail.com>"

# === Allowed origins for CORS (comma-separated) ===
ALLOWED_ORIGINS=https://www.pilgrimspath.io,https://pilgrimspath.io
